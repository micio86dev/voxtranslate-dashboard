# Base URL of the VoxTranslate Rust API (Railway), without a trailing slash.
# e.g. https://api.voxtranslate.app  (locally: http://localhost:3001)
PUBLIC_API_BASE=http://localhost:3001

# Google OAuth client id (same project as the consumer app) for Sign in with Google.
PUBLIC_GOOGLE_CLIENT_ID=
